/*
 * Shared safety limits for small characterization runs.
 */

params {
  max_items = 256
  max_batch_size = 64
  max_sleep_seconds = 300
  max_cpu_seconds = 300
  max_mem_mb = 4096
  max_io_mb = 256
}

process {
  cpus = 1
  memory = '1 GB'
  time = '15 min'
}
